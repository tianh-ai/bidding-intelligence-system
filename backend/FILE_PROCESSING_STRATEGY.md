# 📋 投标文件智能分类与处理方案

## 一、总体架构

```
┌─────────────────────────────────────────────────────────────┐
│  用户上传文件                                                 │
│  (PDF/Word/图片)                                             │
└──────────────────┬──────────────────────────────────────────┘
                   ↓
┌─────────────────────────────────────────────────────────────┐
│  1️⃣ 智能分类阶段                                             │
│  SmartDocumentClassifier                                     │
│  - 检测文件类型（证件/财务/标书等）                           │
│  - 分析页面结构（文本/扫描/图片）                             │
│  - 确定处理策略                                              │
└──────────────────┬──────────────────────────────────────────┘
                   ↓
     ┌─────────────┼─────────────┬──────────────┬──────────────┐
     ↓             ↓             ↓              ↓              ↓
┌─────────┐  ┌─────────┐  ┌──────────┐  ┌─────────────┐  ┌──────┐
│ 主标书  │  │扫描PDF  │  │混合PDF   │  │财务/证件    │  │图片  │
│ 完整解析 │  │OCR识别  │  │混合方式  │  │仅保存      │  │元数据│
└────┬────┘  └────┬────┘  └────┬─────┘  └──────┬─────┘  └──┬───┘
     │           │            │              │           │
     ├───────────┴────────────┴──────────────┴───────────┘
     ↓
┌─────────────────────────────────────────────────────────────┐
│  2️⃣ 处理执行阶段                                             │
│  DocumentProcessor + 对应策略                               │
└──────────────────┬──────────────────────────────────────────┘
                   ↓
┌─────────────────────────────────────────────────────────────┐
│  3️⃣ 存储与索引                                               │
│  - 提取的文本/目录入库                                       │
│  - 原始文件分类存储                                          │
│  - 构建搜索索引                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、文件分类规则

### 2.1 主标书（MAIN_PROPOSAL）
**识别特征：**
- 包含目录结构
- 大量文本内容
- 多个章节

**处理策略：**
```
提取文本 → 识别目录 → 解析章节 → 建立索引
```

**输出：**
- 章节结构
- 完整内容
- 搜索索引

---

### 2.2 证件/资质（LICENSE, CERTIFICATE）
**识别关键词：**
```
营业执照、工商、统一社会信用
资质、证书、认证、许可证
身份证、护照、居住证
```

**处理策略：**
```
识别 → 不解析 → 仅保存PDF → 记录位置和页数
```

**存储位置：**
```
documents/
├── licenses/          # 营业执照
├── certificates/      # 资质证书
└── id_proofs/        # 身份证明
```

**记录信息：**
- 文件名
- 页数
- 尺寸
- 扫描比例

---

### 2.3 财务报告（FINANCIAL_REPORT）
**识别特征：**
```
- 页数 ≥ 5（通常多页）
- 包含关键词：财务报表、资产负债表、利润表、年报
- 文件名包含：财务、报表、年报、审计
```

**特殊处理：按年份分组**
```
自动识别年份 → 分组 → 按年份目录存储
```

**存储结构：**
```
documents/financial_reports/
├── 2023/
│   ├── company_A_2023.pdf
│   └── company_B_2023.pdf
├── 2022/
│   └── company_A_2022.pdf
└── 2021/
    └── company_A_2021.pdf
```

**关键处理：**
- ❌ 不解析内容
- ✅ 记录年份
- ✅ 保存完整PDF
- ✅ 索引便于检索

---

### 2.4 业绩报告（PERFORMANCE_REPORT）
**识别关键词：**
```
业绩、工程、施工、项目完成、中标
```

**处理策略：**
```
识别 → 不解析 → 仅保存PDF → 记录位置
```

**存储位置：**
```
documents/performance_reports/
├── 项目A_业绩报告.pdf
└── 项目B_中标文件.pdf
```

---

### 2.5 扫描PDF（SCAN_PDF）
**识别特征：**
```
- 扫描页占比 > 80%
- 文本提取 < 100字/页
- 主要内容为图像
```

**处理策略：**
```
检测扫描页 → PaddleOCR识别 → 提取文本 → 目录解析
```

**关键特性：**
- 自动使用PaddleOCR
- 支持中文识别
- 识别置信度记录

---

### 2.6 混合PDF（MIXED_PDF）
**识别特征：**
```
- 扫描页占比 20-80%
- 既有文本页又有扫描页
```

**处理策略：**
```
文本页: 直接提取
扫描页: OCR识别
结果: 合并
```

**输出统计：**
```json
{
  "total_pages": 50,
  "text_pages": 35,
  "scan_pages": 15,
  "average_confidence": 0.87
}
```

---

### 2.7 图片文件（IMAGE_ONLY）
**处理策略：**
```
检测 → 提取元数据 → 不保存图片 → 记录位置
```

**提取元数据：**
```python
{
  'filename': 'image.jpg',
  'size': (2400, 1800),      # 宽x高
  'format': 'JPEG',
  'file_size': 1024000,       # 字节
  'dpi': 300
}
```

---

## 三、关键处理逻辑

### 3.1 文本提取流程

```python
# 混合提取器流程
async def extract_from_pdf_page(page):
    # Step 1: 尝试直接文本提取（快速）
    text = page.extract_text()
    
    if len(text) > 100:
        return {'text': text, 'method': 'direct', 'confidence': 0.95}
    
    # Step 2: 文本不足 → 使用OCR
    image = render_page_to_image(page)
    ocr_text = paddle_ocr.recognize(image)
    
    return {'text': ocr_text, 'method': 'ocr', 'confidence': 0.85}
```

### 3.2 年份识别逻辑

```python
# 从财务报告中识别年份
YEAR_PATTERN = r'(19|20)\d{2}|(\d{4})年|(\d{4}年度)'

# 自动分组
years = extract_years(first_page_text)
# 结果：[2023, 2022, 2021]

# 存储位置
storage = f'financial_reports/{year}/'
```

### 3.3 证件识别逻辑

```python
def is_certificate(text, filename):
    # 检查文件名
    if '营业执照' in filename:
        return True
    
    # 检查内容
    if '统一社会信用' in text:
        return True
    
    return False

# 识别后直接保存，不解析
```

---

## 四、系统流程

### 4.1 上传与分类

```python
# 1. 用户上传文件
pdf_file = upload_file()

# 2. 自动分类
classifier = SmartDocumentClassifier()
analysis = classifier.classify(pdf_path, filename)

# 3. 获取分类信息
print(f"文件类型: {analysis.file_type.value}")
print(f"处理策略: {analysis.processing_strategy}")
print(f"说明: {analysis.processing_note}")

# 结果示例：
# 文件类型: financial_report
# 处理策略: group_by_year_store
# 说明: 财务报告 [2023, 2022]，按年份分组存储，不解析
```

### 4.2 处理执行

```python
# 4. 选择对应处理策略
processor = DocumentProcessor()
result = await processor.process(file_path, filename)

# 处理结果：
{
    'status': 'success',
    'file_type': 'main_proposal',
    'chapters': [...],
    'classification': {...},
    'processing_time': '2025-12-10T12:34:56'
}
```

---

## 五、引入的新依赖

### 5.1 requirements.txt

```
# OCR & Image Processing
paddlepaddle==2.6.1          # PaddlePaddle深度学习框架
paddleocr==2.7.0.3           # PaddleOCR识别引擎
pillow==10.1.0               # 图片处理
```

### 5.2 模型下载

PaddleOCR首次运行时会自动下载：
- 中文检测模型 (~50MB)
- 中文识别模型 (~150MB)

**缓存位置：** `~/.paddleocr/`

### 5.3 GPU支持（可选）

```bash
# 使用GPU加速
pip install paddlepaddle-gpu

# 初始化时配置
ocr = PaddleOCR(use_gpu=True, lang='ch')
```

---

## 六、性能指标

### 6.1 提取速度

| 文件类型 | 处理方式 | 速度 |
|---------|---------|------|
| 文本PDF (10页) | 直接提取 | ~0.1秒 |
| 扫描PDF (10页) | OCR | ~5-10秒 |
| 混合PDF (10页) | 混合 | ~3-5秒 |
| 财务报告 (50页) | 分类存储 | ~0.2秒 |
| 图片 | 元数据 | <0.1秒 |

### 6.2 精度指标

| 方式 | 置信度 | 适用场景 |
|------|--------|---------|
| 直接文本 | 95%+ | 规范PDF |
| PaddleOCR | 85-92% | 扫描PDF |
| 混合 | 80-90% | 混合PDF |

---

## 七、集成示例

### 7.1 在文件上传路由中集成

```python
@router.post("/upload")
async def upload_files(files: List[UploadFile]):
    processor = DocumentProcessor()
    results = []
    
    for file in files:
        # 保存文件
        file_path = save_file(file)
        
        # 智能处理
        result = await processor.process(file_path, file.filename)
        results.append(result)
        
        # 根据类型存储
        if result['file_type'] == 'main_proposal':
            # 提取目录到数据库
            save_chapters(result['chapters'])
        
        elif result['file_type'] == 'financial_report':
            # 按年份分类存储
            year = result['classification']['financial_years'][0]
            move_file(file_path, f'financial_reports/{year}/')
        
        elif result['file_type'] in ['license', 'certificate']:
            # 仅保存，记录位置
            move_file(file_path, f'documents/{result["file_type"]}/')
    
    return {'status': 'success', 'results': results}
```

### 7.2 数据库记录

```python
# 新增字段到uploaded_files表
ADD COLUMN IF NOT EXISTS document_type VARCHAR(50);
ADD COLUMN IF NOT EXISTS processing_strategy VARCHAR(100);
ADD COLUMN IF NOT EXISTS classification_result JSONB;
ADD COLUMN IF NOT EXISTS detected_years INT[];  -- 财务报告年份
```

---

## 八、实施清单

- [ ] 添加PaddleOCR到requirements.txt
- [ ] 创建smart_document_classifier.py
- [ ] 创建ocr_extractor.py
- [ ] 创建document_processor.py
- [ ] 更新文件上传路由集成处理
- [ ] 创建文件分类存储目录结构
- [ ] 添加数据库字段记录分类信息
- [ ] 测试各类文件的识别和处理
- [ ] 优化OCR性能（缓存、批处理）
- [ ] 创建前端显示处理进度

---

## 九、FAQ

**Q: 是否所有PDF都需要OCR？**
A: 否。仅在直接文本提取失败时才使用OCR，可节省资源。

**Q: 财务报告多长？**
A: 通常10-100页，系统自动分组存储不解析。

**Q: 如何处理多份业绩报告？**
A: 识别为performance_report类型，按日期或项目分类存储。

**Q: 图片占用空间大吗？**
A: 仅保存元数据，不保存图片本身，占用空间极小。

**Q: PaddleOCR对英文支持如何？**
A: 默认中文优化，英文识别精度约85%，可使用多语言模型提升。
