# ============================================
# 后端环境变量模板 - backend/.env
# ⚠️ 请勿直接修改此文件！
# 正确的值已在 config-guard.sh 中定义
# ============================================

# 数据库配置（Docker PostgreSQL）
DB_HOST=localhost
DB_PORT=5433                    # ⚠️ Docker 映射端口，不是 5432！
DB_USER=postgres
DB_PASSWORD=postgres123         # ⚠️ 与 docker-compose.yml 保持一致
DB_NAME=bidding_db              # ⚠️ 不是 postgres

# Redis 配置
REDIS_HOST=localhost
REDIS_PORT=6379                 # ⚠️ 代码默认 6379，Docker 暴露 6380

# OpenAI API
OPENAI_API_KEY=your-api-key-here
OPENAI_MODEL=gpt-4-turbo

# 文件存储（SSD 路径）
UPLOAD_DIR=/Volumes/ssd/bidding-data/uploads
PARSED_DIR=/Volumes/ssd/bidding-data/parsed
ARCHIVE_DIR=/Volumes/ssd/bidding-data/archive

# 日志配置
LOG_LEVEL=INFO
LOG_FILE=/Volumes/ssd/bidding-data/logs/app.log

# ============================================
# 前端环境变量模板 - frontend/.env
# ============================================

# API 配置
VITE_API_URL=http://localhost:18888   # ⚠️ Docker 后端端口，不是 8000！

# 默认管理员账号
VITE_DEFAULT_ADMIN_USERNAME=admin
VITE_DEFAULT_ADMIN_PASSWORD=bidding2024
